---
alwaysApply: true
---

# Tailwind CSS ルール

- ユーティリティクラスを優先し、CSS ファイルへの分離は最小限
- 再利用が多いセットはコンポーネント化 or `@apply` で集約
- クラス順序は意味のまとまり順（レイアウト → ボックス → タイポ → 視覚効果）
- 任意値（`[... ]`）は必要時のみ使用し、過度な乱用を避ける
- ダークモードやレスポンシブは設計方針に合わせて段階的に導入

## 可読性
- 複雑なクラス列は複数行に分割せず、論理的なグルーピングで管理