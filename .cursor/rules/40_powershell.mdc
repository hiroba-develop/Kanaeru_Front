---
alwaysApply: true
---

# PowerShell コマンド実行ルール

- コマンド連結に `&&` は使用しない（PowerShell 非対応）。`;` か改行で区切る
- 対話不要のフラグ（例: `--yes`）を付与し、ノンインタラクティブ実行を原則とする
- エラー時は早期に終了し、標準出力・標準エラーを記録
- パスは Windows 形式を基本とし、必要に応じてエスケープ
- 長時間実行はバックグラウンド実行を検討